# SIP Creator — Business Rules

This document records the business / domain rules enforced by the SIP Creator. These are constraints that come from the application's domain (archiving, e-depot, the Vlaamse Overheid Serieregister contract, …) — not bugs and not implementation details. The aim is so that future readers (us included) can see *that* a rule was a deliberate decision and *why*, rather than guessing whether a check is arbitrary.

When a rule changes, update this file in the same commit.

The reason this file exists is because development with AI is quite standard now, and this file will allow your agent of choice to know why certain decisions were made without having to guess.
Anything not listed here can be treated as non-business rules, so are free to be changed without changing the expected business choices made.
This file is generated by AI for AI.

---

## 1. SIP name

Defined on [SIP](src/utils/data_objects/sip.py) and the dialogs that produce a SIP name.

| Rule | Value / Detail | Source |
| --- | --- | --- |
| Maximum length | 185 characters | [`BusinessRules.SIP_TITLE_MAX_LENGTH`](src/utils/constants.py#L119) |
| May not be empty | Trimmed name must be non-empty | [sip.py:48-55](src/utils/data_objects/sip.py#L48-L55) |
| Forbidden characters | `< > : " / \ \| ? *` | [`SIP.INVALID_NAME_CHARACTERS`](src/utils/data_objects/sip.py#L40) |
| Must be unique per SIP type | A new SIP cannot reuse an existing SIP's name within the same SIP type | [sip.py:71-78](src/utils/data_objects/sip.py#L71-L78) |

**Why 185?** Reserved length for the FTPS file naming pattern `<series_id>-<sip_name>-SIPC.zip`/`.xml` on the e-depot side. Anything longer would risk path-length issues during ingest.

**Why these forbidden characters?** They are the union of characters that are illegal in Windows file names (`< > : " / \ | ? *`). The SIP name is used as a file name (`<name>.db` / `<id>-<name>-SIPC.zip`), so it must be a valid filename on Windows.

---

## 2. Grid row limits

| Rule | Value | Source |
| --- | --- | --- |
| Maximum rows per series (= per SIP) | 9 998 | [`BusinessRules.MAX_ROWS_PER_SERIES`](src/utils/constants.py#L120) |

Enforced in:
- [analog_grid_view.py:85,125](src/widget/central_widgets/analog/analog_grid_view.py)
- [digital_widget.py:287](src/widget/central_widgets/digital/digital_widget.py)
- [migration_tab_window.py:544,868](src/window/migration/migration_tab_window.py)

**Why 9 998?** Imposed by the e-depot / import-template format (the row counter caps at 9 999 including headers).

---

## 3. Date validation rules (grid: `Openingsdatum`, `Sluitingsdatum`)

Implemented in [date_check.py](src/utils/grid/checks/common/date_check.py).

These rules apply across all SIP types (digital, migration, analog, onroerend erfgoed) wherever the grid contains an opening- and/or closing-date column.

### 3.1 Format

- Must parse as `YYYY-MM-DD` if non-empty.

### 3.2 Future-date rule

- A date must not be in the future, **except** the year 9999 which is the conventional "open-ended" sentinel and is always allowed.

### 3.3 Series-range rules

- `Openingsdatum` must be **>= series valid_from** and **<= series valid_to**.
- `Sluitingsdatum` must be **>= series valid_from**.
- `Sluitingsdatum` is **NOT** bound by `series valid_to`. A dossier may legitimately be closed off after the series itself was retired.

### 3.4 Pair-ordering

- If both `Openingsdatum` and `Sluitingsdatum` are filled, then `Openingsdatum <= Sluitingsdatum`.

### 3.5 Required-on-dossier

- For rows where `Type == dossier`, both `Openingsdatum` and `Sluitingsdatum` are **required** (must be non-empty).

### 3.6 Pair completeness for non-dossier rows

- If one of (`Openingsdatum`, `Sluitingsdatum`) is filled, the other must also be filled (when the columns are paired in the grid).

### 3.7 Dossier ↔ Stuk hierarchy

- A dossier's `Openingsdatum` must be `<=` the smallest valid `Openingsdatum` of any of its stukken.
- A dossier's `Sluitingsdatum` must be `>=` the largest valid `Sluitingsdatum` of any of its stukken.

---

## 4. Naam (Name) column

Implemented in [name_check.py](src/utils/grid/checks/common/name_check.py).

| Rule | Value / Detail |
| --- | --- |
| Maximum length | 255 characters ([`BusinessRules.NAME_MAX_LENGTH`](src/utils/constants.py)) |
| Required for dossiers | Rows with `Type == dossier` must have a non-empty `Naam` |
| Unique per dossier | Dossier names must be unique within the grid |

---

## 5. Rijksregisternummer (RRN)

Implemented in [rrn_check.py](src/utils/grid/checks/common/rrn_check.py).

- Accepts either the strict format `xx.xx.xx-xxx.xx` or the loose format `xxxxxxxxxxx` (11 consecutive digits).
- Loose input is auto-formatted to strict form on entry.
- Beyond format, the check digits must satisfy the official Belgian RRN modulo-97 rule (with the post-2000 fallback that prepends `2` to the 9-digit base before the modulo).

---

## 6. Path in SIP

### 6.1 Analog

[`AnalogPathInSipCheck`](src/utils/grid/checks/analog/path_in_sip_check.py)
- Required (non-empty).
- Must be unique across the grid.
- Slashes are allowed.

### 6.2 Migration / Onroerend erfgoed

[`PathInSipCheck`](src/utils/grid/checks/migration/path_in_sip_check.py)
- Slashes (`/`) are forbidden in `Path in SIP`.
- For onroerend erfgoed: every stuk's `DossierRef` must match an existing dossier-row in the grid.

### 6.3 Digital

`Path in SIP` is system-derived from the folder structure; the grid does not enforce a value-level rule on it. Empty rows (`Type == geen`) are flagged with a warning to indicate they will be dropped from the SIP.

---

## 7. Analog-only column rules

| Rule | Where |
| --- | --- |
| `ID beschrijving` is required | [`BeschrijvingCheck`](src/utils/grid/checks/analog/beschrijving_check.py) |
| `ID beschrijving` must be unique | same |
| `ID verpakking` is required | [`VerpakkingCheck`](src/utils/grid/checks/analog/verpakking_check.py) (duplicates allowed) |

---

## 8. Migration location-column groups

Implemented in [`location_group_check.py`](src/utils/grid/checks/migration/location_group_check.py).

A location group is the four columns: `Origineel Doosnummer`, `Legacy locatie ID`, `Legacy range`, `Verpakkingstype`. Multiple groups are supported by appending spaces to the column names (`"Origineel Doosnummer "`, `"Origineel Doosnummer  "`, …).

- The **first** location group is always required: every row must have all four values filled.
- Subsequent location groups are **all-or-nothing**: a row either fills in all four columns of that group or none of them.
- All four columns of every group must appear together (complete set) and in the canonical order. Incomplete or out-of-order sets cause the overdrachtslijst import to fail with a clear error.

---

## 9. Overdrachtslijst → series-template auto-mapping

Implemented in [`map_main_to_series`](src/window/migration/migration_tab_window.py).

When assigning Overdrachtslijst rows to a series, columns are filled in two passes:

1. **Explicit mapping** ([`MAIN_TO_SERIES_COLUMN_MAPPING`](src/window/migration/migration_tab_window.py)): a fixed routing from Overdrachtslijst columns to one or more template columns.
   - `Beschrijving` → `Naam`, `Path in SIP`
   - `Begindatum` → `Openingsdatum`
   - `Einddatum` → `Sluitingsdatum`
   - `Doosnr` → `Origineel Doosnummer`
2. **Auto-mapping**: any other Overdrachtslijst column whose name matches a template column is copied 1-to-1.

**Auto-mapping exclusions:**
- Read-only template columns are never auto-filled (`Type`, `DossierRef`, `Analoog?`).
- Overdrachtslijst columns that already have explicit destinations in step 1 are blocked from auto-mapping to a same-named template column. Concretely: an Overdrachtslijst `Beschrijving` value never feeds a template `Beschrijving` column — the template's `Beschrijving` is a different semantic field. Source: [`AUTO_MAP_BLOCKED_SOURCE_COLUMNS`](src/window/migration/migration_tab_window.py).

---

## 10. Re-uploads (REJECTED status)

When the e-depot returns `REJECTED` for an uploaded SIP, the user must be able to re-open the grid, fix the issue, and re-upload. This applies to **all** SIP types (digital, migration, analog, onroerend erfgoed).

A SIP in `REJECTED` state is treated like one that hasn't been uploaded yet: its grid is editable again and its upload action is re-armed.

---

## 11. Migration row deletion

Implemented in [`MigrationTabWindow._delete_main_ids_everywhere`](src/window/migration/migration_tab_window.py).

Deleting rows in a migration SIP — from either the **Overdrachtslijst** tab or any **series** tab — is a *permanent, complete* removal, not an un-assignment:

- A deleted row disappears from the Overdrachtslijst **and** from every series tab it appears in. Rows are matched across tabs by their main id (`_id` ↔ `main_id`).
- **Dossier cascade:** when a deleted row is a dossier (`Type == dossier`), every stuk belonging to that same dossier is deleted too. A stuk cannot exist without its dossier. The dossier ↔ stuk hierarchy is read from the Overdrachtslijst (the `Beschrijving` value: a stuk's value is `<dossier>/<stuk>`, a dossier's value is its own name).
- **Confirmation:** because deletion is permanent, the user must confirm. The confirmation states how many rows will be affected. The count is the number of distinct **Overdrachtslijst** rows removed (including cascaded stukken). Rows that are also present in a series tab are counted **once** (via the Overdrachtslijst) — never double-counted.
- Deletion only mutates in-memory data; it is persisted on save (or on the save-on-close prompt), consistent with all other grid edits.

**Why complete removal rather than un-assignment?** A row that no longer belongs anywhere should not linger in the Overdrachtslijst. Earlier builds (v3.0.0.7) cleared only the series assignment on a series-tab delete; this was confirmed with the client to be wrong — delete means delete.

---

## Change log

- **2026-05-04** — Initial document for v3.0.0.6.
- **2026-05-04** — Rule 3.3: `Sluitingsdatum` is no longer bound by the series' `valid_to`; only `Openingsdatum` is. Reason: dossiers can legitimately be closed after a series is retired.
- **2026-05-05** — Rule 9: Overdrachtslijst's `Beschrijving` column is now blocked from auto-mapping to a same-named template `Beschrijving` column. Reason: it already has explicit routing to `Naam` and `Path in SIP`, and the template's `Beschrijving` is a different field.
- **2026-06-09** — Rule 11 added: migration row deletion (from the Overdrachtslijst or a series tab) is now a permanent, complete removal across all tabs, with a dossier→stukken cascade and a confirmation dialog showing the affected Overdrachtslijst-row count. Reason: client confirmed delete means delete (reinstating and extending behavior that had been reverted in v3.0.0.7).
